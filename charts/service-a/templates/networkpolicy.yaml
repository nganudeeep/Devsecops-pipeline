{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: service-a

spec:
  podSelector:
    matchLabels:
      app: service-a

  policyTypes:
    - Ingress

  ingress:
    - from:
        - podSelector: {}
{{- end }}